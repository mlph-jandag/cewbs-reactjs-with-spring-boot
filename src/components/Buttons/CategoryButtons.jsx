import React from 'react'

const CategoryButtons = (props) => {
    return (
        <div>
            test
        </div>
    )
}

props.defaultProps = {
    hasAll: false,
    top: 5
};

export default CategoryButtons;